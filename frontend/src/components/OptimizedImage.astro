---
import { generateCloudinaryURL } from "../lib/generate-cloudinary-url";

interface Props {
  src: string;
  alt: string;
  class?: string;
  // width: string;
  // height: string;
}

const { src, alt, ...props } = Astro.props;;
const optimized = generateCloudinaryURL(src);
console.log(src, "what is this", optimized)
---

<img src={optimized} loading="lazy" {...props} />
